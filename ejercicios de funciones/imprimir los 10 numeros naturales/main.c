#include <stdio.h>
#include <stdlib.h>

int main()
{
    //creamos un void y lo nombramos numero
void numero() {
    //creamos una variable llamada i que va a tener un valor de 1
    int i = 1;
    //creamos un blucle y decimos que i tiene que se menos a i que es 1 para detenerse
    while (i<=10){
            //imprimimos
        printf("%d\n", i);
        i++;
    }
}
int main()
{
    numero();

    return 0;
}
